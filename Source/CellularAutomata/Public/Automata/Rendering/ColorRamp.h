#pragma once

#include "CoreMinimal.h"
#include "ColorRamp.generated.h"

/** В каком пространстве проводится прямая между соседними опорными цветами
 *  рампы (AAutomataOrchestrator::AgeColors/DecayColors).
 *
 *  Вопрос не косметический: прямая между двумя цветами в разных пространствах
 *  проходит через РАЗНЫЕ промежуточные цвета, и именно они занимают почти всю
 *  рампу - опорных точек единицы, а промежуточных значений 256.
 *
 *  Выбор здесь ортогонален выбору EColorRampCurve: пространство определяет,
 *  где проходит прямая, кривая - как по ключам двигаться. Комбинируются
 *  свободно. */
UENUM(BlueprintType)
enum class EColorRampSpace : uint8
{
	/** Линейный RGB - историческое поведение проекта, менять умолчание на
	 *  что-то другое значило бы молча переписать вид всех уже настроенных рамп.
	 *
	 *  Физически это правильное смешивание СВЕТА (так складываются два
	 *  источника), но для градиента плохо: прямая срезает угол через
	 *  внутренность RGB-куба, и середина теряет насыщенность - красный в
	 *  зелёный идёт через болотный, синий в жёлтый через блёклый. Шаги при этом
	 *  воспринимаются неравномерно: в тёмной части разница почти не видна. */
	LinearRgb,

	/** Гамма-пространство sRGB: перевести, смешать, вернуть. Так по умолчанию
	 *  смешивает Photoshop, поэтому результат совпадает с тем, что ожидает
	 *  человек, рисовавший рампу в графическом редакторе. Ненасыщенная середина
	 *  никуда не девается, но прогресс яркости выходит ровнее линейного. */
	Srgb,

	/** OKLab - перцептивно равномерное пространство (Björn Ottosson, 2020),
	 *  спроектированное ровно под эту задачу. Грязных середин нет, равные шаги
	 *  по t дают равные ВОСПРИНИМАЕМЫЕ различия.
	 *
	 *  Взято вместо классического CIELAB намеренно: OKLab создавался как
	 *  исправление его известного паразитного сдвига тона в синей области, так
	 *  что для градиентов он строго лучше. */
	Oklab,

	/** Полярная форма OKLab: светлота, насыщенность и УГОЛ тона. Отличается от
	 *  Oklab ровно одним: тон интерполируется по дуге, а не по хорде, поэтому
	 *  насыщенность держится всю дорогу и цвет "поворачивается" вместо того
	 *  чтобы проседать через серое.
	 *
	 *  Дуга берётся всегда КОРОТКАЯ. Длинная дуга дала бы обход через
	 *  противоположную часть круга - иногда это красиво, но это уже не
	 *  "градиент между двумя цветами", а самостоятельный эффект, и отдельного
	 *  значения перечисления он пока не заслужил.
	 *
	 *  У серого и чёрного тон не определён вовсе (насыщенность нулевая); такие
	 *  ключи берут тон соседа, иначе на них рампа дёргалась бы в случайную
	 *  сторону. */
	Oklch
};

/** Как двигаться по опорным цветам. Ортогонально EColorRampSpace. */
UENUM(BlueprintType)
enum class EColorRampCurve : uint8
{
	/** Кусочно-линейно - историческое поведение. На КАЖДОМ опорном цвете
	 *  производная меняется скачком, поэтому при трёх и более ключах на гладкой
	 *  структуре видна чёткая граница ровно там, где стоит ключ. */
	Linear,

	/** Катмулл-Ром: гладкая кривая, проходящая ЧЕРЕЗ все ключи, изломов на них
	 *  нет. Концы обрабатываются дублированием крайних ключей.
	 *
	 *  Сплайн умеет вылетать за пределы диапазона опорных значений, поэтому
	 *  результат обязателен к зажиму - без него получились бы цвета вне гаммы
	 *  (а в OKLab ещё и не имеющие обратного преобразования). */
	CatmullRom
};

/** Выборка цвета из рампы опорных цветов - свободные функции, а не метод
 *  оркестратора, по той же причине, что и namespace GenerationHistory: это
 *  чистая функция от массива и числа, ей не нужны ни актор, ни тик, ни рендер,
 *  и потому она проверяется автотестом напрямую.
 *
 *  Считается 256 раз на рендер при построении таблицы (см.
 *  AAutomataOrchestrator::BuildAgeColorLut()), а не на клетку, поэтому цена
 *  алгоритма здесь роли не играет вовсе: хоть кубические корни OKLab, хоть
 *  сплайн - на фоне миллионов клеток это ноль. Именно поэтому выбор
 *  пространства и кривой можно делать по внешнему виду, а не по стоимости. */
namespace ColorRamp
{
	/** T зажимается в [0,1] и растягивается на весь список ключей: 0 - первый
	 *  ключ, 1 - последний, промежуточные распределены РАВНОМЕРНО (при трёх
	 *  ключах второй приходится ровно на T=0.5).
	 *
	 *  Пустой список - белый (а не отказ): это нормальное промежуточное
	 *  состояние настройки, см. doc-comment AgeColors. Один ключ - он же.
	 *
	 *  ГАРАНТИЯ, на которую опирается автотест: в самих опорных точках
	 *  результат совпадает с ключом при ЛЮБЫХ пространстве и кривой. Именно она
	 *  ловит ошибку туда-обратного преобразования, которая иначе проявляется
	 *  как еле заметный сдвиг оттенка по всей рампе. */
	CELLULARAUTOMATA_API FLinearColor Sample(const TArray<FLinearColor>& Keys, float T,
											 EColorRampSpace Space, EColorRampCurve Curve);
}
