#pragma once

#include "CoreMinimal.h"
#include "Templates/UniquePtr.h"

class FCellGrid;
class FDenseCellGrid;

/** Огрубление сетки: K x K x K мелких клеток сливаются в одну крупную.
 *  Plain namespace-функция, не UObject - та же идиома, что CellAging/
 *  CellSelection/CellMeshBuilder.
 *
 *  Зачем: полный бейк пористой структуры недостижим в принципе, и дело не в
 *  реализации, а в размере геометрии. На 19 млн клеток салфетки выходит
 *  порядка 3 наружных граней на клетку - 57 млн граней, 114 млн
 *  треугольников, около 7 ГБ даже в самом компактном виде на видеокарте.
 *  Огрубление снимает это двумя множителями сразу: клеток становится в K^3
 *  раз меньше, И структура становится плотнее (дыры заполняются), отчего
 *  падает ещё и число граней НА клетку. На факторе 2 задача из невозможной
 *  становится обычной, на 4 - тривиальной.
 *
 *  По сути это то же самое, что уже делает Ghost Shape (см.
 *  AAutomataOrchestrator::bEnableGhostShape) с жёстко зашитым фактором,
 *  равным размеру чанка, - только с произвольным. */
namespace GridDownsample
{
	/** Крупная клетка жива, если жива ХОТЬ ОДНА из K^3 мелких внутри неё.
	 *  Именно "хоть одна", а не голосование большинством: структуры здесь -
	 *  тонкие оболочки в одну-две клетки толщиной, и при большинстве они бы
	 *  просто исчезли. Ценой того, что фигура становится чуть толще
	 *  оригинала, сохраняются силуэт и связность.
	 *
	 *  Factor округляется вверх до степени двойки - тогда деление координат
	 *  (в том числе отрицательных, а они здесь норма) это арифметический
	 *  сдвиг, ровно как в FDenseCellGrid::CellToChunkCoord().
	 *
	 *  Возрасты и состояния угасания НЕ переносятся: огрублённая сетка нужна
	 *  геометрии, а не симуляции - продолжать с неё нельзя и не нужно. */
	CELLULARAUTOMATA_API TUniquePtr<FDenseCellGrid> Downsample(const FCellGrid& Source, const TArray<FIntVector>& Cells, int32 Factor, int32 ChunkSize);
}
