#pragma once

#include "CoreMinimal.h"
#include "SelectionCombineMode.generated.h"

/** Как результат очередного прямоугольного выделения (marquee) комбинируется
 *  с уже накопленным SelectedCells - стандартный пользовательский паттерн
 *  модификаторов: без модификатора - заменить, Shift - добавить, Ctrl -
 *  убрать. Модификатор снимается в момент НАЧАЛА драга
 *  (AGamePlayerController::OnSelectDragStarted()), не отпускания - как в
 *  большинстве редакторов, чтобы режим не "прыгал", если пользователь
 *  отпустил модификатор чуть раньше кнопки мыши. Отдельный заголовок (не
 *  внутри AutomataOrchestrator.h) - та же конвенция, что у ENeighborhood/
 *  EChunkedRenderOrder: подсистема владеет своими типами. */
UENUM(BlueprintType)
enum class ESelectionCombineMode : uint8
{
	/** Новое выделение заменяет старое целиком (поведение без модификатора -
	 *  единственное, что существовало до появления Shift/Ctrl). */
	Replace,

	/** Shift: клетки из прямоугольника добавляются к уже выделенным
	 *  (объединение множеств, без дублей). */
	Add,

	/** Ctrl: клетки из прямоугольника убираются из уже выделенных
	 *  (разность множеств). */
	Subtract
};
