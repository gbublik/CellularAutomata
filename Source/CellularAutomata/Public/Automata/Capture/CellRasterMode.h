#pragma once

#include "CoreMinimal.h"
#include "CellRasterMode.generated.h"

/** Как сворачиваются клетки, попавшие в один и тот же пиксель снимка.
 *
 *  Вдоль луча взгляда лежит много клеток, а пиксель один - правило нужно
 *  всегда. Отдельный заголовок, а не поле внутри AutomataOrchestrator.h - та
 *  же конвенция, что у ENeighborhood/EChunkedRenderOrder/EStateGeneratorType:
 *  подсистема владеет своими типами. */
UENUM(BlueprintType)
enum class ECellRasterMode : uint8
{
	/** Ближайшая к камере клетка - ровно то, что видно на экране. Снимок
	 *  повторяет картинку, только без сглаживания и в своём масштабе. */
	NearestToCamera,

	/** Чистая двухцветная маска: есть клетка или нет, без оттенков вовсе.
	 *  Самый дешёвый режим и самый прямой ответ на "нужна маска" - цвет клетки
	 *  здесь не участвует, поэтому рампа, фильтры возраста и угасание на
	 *  результат не влияют. */
	Silhouette,

	/** Сколько живых клеток лежит вдоль луча, нормированное в градацию серого
	 *  от самого тонкого места к самому толстому.
	 *
	 *  Это единственные данные, которых на экране не видно в принципе: карта
	 *  толщины структуры, готовая как height/displacement для материала.
	 *  Цвет клеток здесь не используется вовсе - только их количество. */
	Thickness
};
