#pragma once

#include "CoreMinimal.h"
#include "SliceTileMode.generated.h"

/** Достраивать ли снимок до бесшовного тайла зеркальным отражением.
 *
 *  Отражение стыкуется всегда, без всяких условий на сам узор: вдоль линии
 *  отражения пиксели совпадают по построению. Цена - результат обязательно
 *  симметричен; узор, который тайлится БЕЗ симметрии, требует считать автомат
 *  на торе, а это уже свойство симуляции, а не съёмки.
 *
 *  Крайний ряд при отражении не дублируется (см. CellRasterizer::MakeTile()) -
 *  иначе на шве получалась бы двойная линия. */
UENUM(BlueprintType)
enum class ESliceTileMode : uint8
{
	/** Снимок как есть. */
	None,

	/** Отразить по горизонтали - тайл шириной 2W-2. */
	MirrorX,

	/** Отразить по вертикали - тайл высотой 2H-2. */
	MirrorY,

	/** Отразить по обеим осям - квадратный тайл (2W-2)x(2H-2), обычный
	 *  вариант для орнамента. */
	MirrorBoth
};
