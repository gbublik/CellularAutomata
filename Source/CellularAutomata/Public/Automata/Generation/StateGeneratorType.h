#pragma once

#include "CoreMinimal.h"
#include "StateGeneratorType.generated.h"

/** Каким геометрическим построением создаётся начальное состояние сетки -
 *  см. StateGenerators::Generate() и AAutomataOrchestrator::GenerateState()
 *  (хоткей Y). Отдельный заголовок, а не поле внутри AutomataOrchestrator.h -
 *  та же конвенция, что у ENeighborhood/EChunkedRenderOrder/
 *  ESelectionCombineMode: подсистема владеет своими типами.
 *
 *  Перечисление ПЛОСКОЕ, а не "семейство + подтип": Shift+Y тогда это ровно
 *  (uint8 + 1) % Num (идиома CycleChunkedRenderOrder()), в EditCondition полей
 *  FStateGeneratorParams - одно сравнение вместо двух, а в HUD - один
 *  выпадающий список вместо двух связанных. Семейство существует только как
 *  поле FStateGeneratorPreset::FamilyName, то есть как данные для группировки
 *  в UMG, а не как тип.
 *
 *  Все генераторы ЧИСТО ГЕОМЕТРИЧЕСКИЕ: они не читают BirthCounts/
 *  SurvivalCounts и не прогоняют пробных шагов. Стабильна ли построенная
 *  структура под текущим правилом - вопрос к правилу, а не к генератору;
 *  подобрать правило помогает гистограмма соседей (см.
 *  FStateGeneratorParams::bAnalyzeNeighborCounts).
 *
 *  ПОРЯДОК: дописывать только в конец соответствующего семейства.
 *  Переупорядочивать безопасно, пока значение уходит в .casave строкой (как
 *  ENeighborhood), а вот удалять и переименовывать элементы - нет. */
UENUM(BlueprintType)
enum class EStateGeneratorType : uint8
{
	/** Случайные клетки в шаре радиуса Radius - ровно то, что делал (и делает)
	 *  GenerateRandom(): Amount БРОСКОВ с reject-sampling, дубли не
	 *  отслеживаются, поэтому клеток получается меньше, чем бросков. Стоит
	 *  первым и нулевым намеренно: это поведение по умолчанию и то, что
	 *  получают BeginPlay(), хоткей N и откат R. */
	RandomBall,

	// --- Решётка / кристалл -------------------------------------------------

	/** Кубы BlockSize^3 в узлах решётки с шагом Period. При Period >=
	 *  BlockSize + 2 каждая живая клетка блока 2x2x2 видит ровно 7 соседей по
	 *  Moore, а примыкающие пустые - не больше 4: широкий зазор между "выживает"
	 *  и "рождается", то есть хороший кандидат на метастабильную структуру. */
	LatticeBlocks,

	/** Шахматная упаковка с ячейкой BlockSize: живая клетка видит 12 соседей по
	 *  Moore, но и примыкающая пустая тоже 12 - правило приходится разводить
	 *  только через разницу множеств S и B. Под окрестностью von Neumann
	 *  вырожден: живых соседей ровно 0. */
	LatticeCheckerboard,

	/** Каркас из рёбер решётки: балки толщиной Thickness вдоль осей, клетка
	 *  ребра принадлежит линиям сразу двух осей. Единственный из решёток, у
	 *  которого число соседей НЕоднородно (тело балки против узла), - его и
	 *  стоит мерить гистограммой перед подбором правила. */
	LatticeFrame,

	/** Набор плоскостей (плит толщиной Thickness с шагом Period) по каждой из
	 *  включённых осей. При Thickness == 1 и одной оси каждая живая клетка
	 *  видит РОВНО 8 соседей по Moore, а пустые над и под плитой - 9: прямой
	 *  3D-аналог большой стабильной 2D-решётки, из которой выбитая клетка
	 *  запускает цепную реакцию. */
	LatticePlanes,

	// --- Заполненные тела ---------------------------------------------------

	/** Сплошной параллелепипед [-Extent, +Extent]. */
	SolidBox,

	/** Сплошной шар радиуса Radius. */
	SolidSphere,

	/** Полый шар: стенка толщиной Thickness внутри радиуса Radius. */
	SphereShell,

	/** Полая коробка: стенка толщиной Thickness внутри [-Extent, +Extent]. */
	BoxShell,

	// --- Шум и кластеры -----------------------------------------------------

	/** Равномерный шум: каждая клетка области оживает с вероятностью Density.
	 *  В отличие от RandomBall даёт ТОЧНУЮ ожидаемую плотность - клетки
	 *  перебираются, а не бросаются, поэтому дублей нет. */
	NoiseUniform,

	/** Perlin-шум: живы клетки, где значение поля выше NoiseThreshold. Даёт
	 *  связные "облака" вместо россыпи одиночек. */
	NoisePerlin,

	/** ClusterCount шарообразных зёрен случайного радиуса, каждое заполнено с
	 *  плотностью Density. */
	NoiseClusters,

	// --- Симметричные затравки ----------------------------------------------

	/** Мелкое случайное ядро CoreExtent, размноженное преобразованиями
	 *  симметрии (см. ESeedSymmetry). Классический способ получить из этих
	 *  правил красивые симметричные фракталы вместо бесформенной кляксы. */
	SymmetricSeed,

	// --- Перенос из двумерной жизни -----------------------------------------

	/** Классический двумерный паттерн Game of Life (ELifePattern), выдавленный
	 *  на Thickness слоёв по Z.
	 *
	 *  Единственный генератор, чей смысл завязан на КОНКРЕТНОЕ правило, - и
	 *  всё же он остаётся чистой геометрией, как и остальные: правило он не
	 *  читает и пробных шагов не делает, а нужное (5,7/6 для двух слоёв) лежит
	 *  готовым пресетом в RulePresets. Почему именно эта пара чисел и почему
	 *  слоёв должно быть ровно два - в doc-comment ELifePattern. */
	LifePattern
};
