#pragma once

#include "CoreMinimal.h"
#include "CellParityFilter.generated.h"

/** Отбор клеток по чётности суммы координат при генерации начального
 *  состояния - способ получить ГРАНЕЦЕНТРИРОВАННУЮ КУБИЧЕСКУЮ решётку (ГЦК,
 *  она же плотная упаковка шаров) без единой строки новой геометрии.
 *
 *  Почему это работает. ГЦК-решётка - это в точности подмножество
 *  {(x,y,z) из Z^3 : x+y+z чётно}, а её 12 ближайших соседей - в точности
 *  смещения с d^2 == 2, то есть ENeighborhood::Edges (см.
 *  GetNeighborhoodShellMask()). Каждое из этих 12 смещений меняет сумму
 *  координат на 0 или +-2, значит чётность инвариантна: засеяв только чётные
 *  клетки, автомат уже никогда с этой подрешётки не сойдёт. При обычном
 *  FCellGrid::GridToWorld() (Cell * CellSize) все 12 соседей оказываются на
 *  одном расстоянии sqrt(2)*CellSize - то есть это НАСТОЯЩАЯ ГЦК-решётка, а не
 *  её имитация, и хранилищу с обеими стратегиями вычисления про это знать не
 *  нужно вовсе: они адресуются целыми координатами и геометрии не видят.
 *
 *  Чётность сохраняют три окрестности из четырнадцати, и осмысленны как ГЦК две
 *  из них: Edges (12, первая координационная сфера) и EdgesFarAxes (18, первая
 *  плюс вторая). Corners (диагонали) чётность на каждом шаге ПЕРЕВОРАЧИВАЕТ -
 *  структура будет прыгать между двумя подрешётками, а не жить на одной;
 *  остальные окрестности уходят с подрешётки за один шаг, и фильтр с ними
 *  бессмыслен (он подействует ровно на начальное состояние и растворится).
 *
 *  Ячейка Вороного этой решётки - ромбододекаэдр, замощающий пространство без
 *  щелей; его габарит равен 2*CellSize, поэтому для честной картинки нужен
 *  меш ромбододекаэдра и AAutomataOrchestrator::CellMeshScaleMultiplier == 2.
 *  С обычным кубом клетки будут соприкасаться только рёбрами - решётка
 *  останется правильной, но выглядеть будет ажурной. */
UENUM(BlueprintType)
enum class ECellParityFilter : uint8
{
	/** Без отбора - обычная кубическая решётка, все клетки на месте. */
	None,

	/** Только клетки с чётной суммой координат - ГЦК-подрешётка. */
	Even,

	/** Только клетки с нечётной суммой координат. Это та же самая ГЦК-решётка,
	 *  сдвинутая на одну клетку: две подрешётки Z^3 конгруэнтны и различить их
	 *  можно лишь по положению относительно нуля. Своё значение у неё есть не
	 *  ради разнообразия, а потому что генераторы строятся ОТНОСИТЕЛЬНО НУЛЯ
	 *  (см. FStateGeneratorParams::Extent - полуразмер, центральная клетка
	 *  ровно в нуле), и у центра узора чётность фиксирована. Симметричная
	 *  затравка с центром в нуле при Even содержит центральную клетку, при Odd
	 *  - дырку на её месте; для правил, чувствительных к ядру, это две разные
	 *  структуры. */
	Odd,

	/** Только клетки, у которых ВСЕ ТРИ координаты одной чётности - это
	 *  ОБЪЁМНОЦЕНТРИРОВАННАЯ КУБИЧЕСКАЯ решётка (ОЦК).
	 *
	 *  Условие другое, чем у Even/Odd: те смотрят на чётность СУММЫ, а здесь
	 *  сравниваются чётности координат между собой. Наборы не вложены друг в
	 *  друга - например (1,1,0) имеет чётную сумму, но не одинаковую чётность
	 *  координат.
	 *
	 *  Почему это ОЦК: клетки со всеми чётными координатами образуют кубическую
	 *  решётку с шагом 2, а клетки со всеми нечётными сидят ровно в центрах её
	 *  кубов - то есть определение ОЦК дословно. Ближайших соседей восемь, по
	 *  диагоналям куба на расстоянии sqrt(3) - это ENeighborhood::Corners;
	 *  следующие шесть на расстоянии 2 по осям - это FarAxes. Обе окрестности
	 *  замкнуты на этой подрешётке: диагональ переворачивает чётность всех трёх
	 *  координат сразу (все чётные <-> все нечётные, и то и другое внутри
	 *  набора), а дальняя ось меняет одну координату на 2, не трогая чётность
	 *  вовсе. Значит осмысленные окрестности здесь - Corners (8) и CornersFarAxes
	 *  (14), и оба уже есть в перечислении.
	 *
	 *  Ячейка Вороного ОЦК - УСЕЧЁННЫЙ ОКТАЭДР: 8 шестиугольных граней смотрят
	 *  на восемь диагональных соседей, 6 квадратных - на шесть осевых, итого 14
	 *  граней при 14 соседях (см. [[Automata/Rendering]] про то же соответствие
	 *  у ромбододекаэдра). Габарит у него, как и у ромбододекаэдра, ровно
	 *  2*CellSize, то есть CellMeshScaleMultiplier снова 2.
	 *
	 *  Вместе с кубом (простая кубическая) и ромбододекаэдром (ГЦК) это третий
	 *  из пяти параллелоэдров Фёдорова, выразимых на целочисленной решётке. */
	SameParity
};
