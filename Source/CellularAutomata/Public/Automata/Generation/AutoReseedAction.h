#pragma once

#include "CoreMinimal.h"
#include "AutoReseedAction.generated.h"

/** Что делать с сидом, забракованным по взрывному росту (см.
 *  AAutomataOrchestrator::bAutoReseedOnExplosion).
 *
 *  Своё перечисление, а не bool, потому что выбор здесь неочевиден и зависит от
 *  того, что именно ищут: вымирание и застой отбраковываются молча, потому что
 *  смотреть там больше не на что, а взрыв - единственный критерий, под который
 *  попадает и то, что может оказаться находкой (фрактальный рост выглядит
 *  ровно как взрыв, пока не присмотришься). */
UENUM(BlueprintType)
enum class EAutoReseedAction : uint8
{
	/** Молча катить следующий сид - как при вымирании и застое, перебор не
	 *  прерывается. */
	Reseed,

	/** Остановить прогон и оставить структуру на экране: дальше решает человек.
	 *  Перебор при этом заканчивается - в том и смысл, "покажи мне вот это". */
	StopSimulation
};
