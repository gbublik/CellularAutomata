#pragma once

#include "CoreMinimal.h"
#include "SeedSymmetry.generated.h"

/** Какими преобразованиями размножается ядро генератора
 *  EStateGeneratorType::SymmetricSeed. Именно перечисление, а не три булевых
 *  флага "зеркалить по X/Y/Z": повороты флагами не выражаются, а именно они
 *  дают самые интересные затравки.
 *
 *  Клетки, лежащие на плоскостях (или осях) симметрии, переходят сами в себя,
 *  поэтому число копий - оценка сверху, а результат обязан дедуплицироваться
 *  через TSet (см. StateGenerators.cpp). */
UENUM(BlueprintType)
enum class ESeedSymmetry : uint8
{
	/** Без размножения - только само ядро, как оно сгенерировалось. */
	None,

	/** Зеркало по X: 2 копии. */
	MirrorX,

	/** Зеркала по X и Y: 4 копии. */
	MirrorXY,

	/** Зеркала по всем трём осям: 8 копий (все комбинации знаков). */
	MirrorXYZ,

	/** Четыре поворота на 90 градусов вокруг оси Z: (x,y) -> (-y,x). Даёт
	 *  "мельницу" - симметрию, которой зеркала не дают. */
	RotateZ4,

	/** Полная симметрия куба (группа Oh): 8 комбинаций знаков на 6
	 *  перестановок осей = 48 копий. Самый плотный вариант - мелкое ядро
	 *  разворачивается в правильный многогранник. */
	FullCubic
};
