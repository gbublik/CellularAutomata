#pragma once

#include "CoreMinimal.h"
#include "CellArrayParams.generated.h"

/** Параметры тиража клеток - AAutomataOrchestrator::ArrayParams, то, чем
 *  управляется ArrayCells() (Ctrl+D).
 *
 *  Это прямой аналог модификатора Array из Blender, с одной разницей: там один
 *  модификатор размножает по одной оси и трёхмерная решётка копий набирается
 *  тремя модификаторами подряд, здесь все три оси заданы сразу (Count), потому
 *  что стека модификаторов у нас нет и накладывать тираж на тираж негде.
 *  Копия с индексом (i,j,k) смещается на (i*ШагX, j*ШагY, k*ШагZ) - тираж
 *  строго ортогональный, косого (сдвигового) размножения нет.
 *
 *  Шаг по каждой оси считается как `round(габарит источника * RelativeOffset)
 *  + ConstantOffset` - те же две слагаемые, что в Blender ("Relative Offset" в
 *  долях габарита плюс "Constant Offset" в единицах), и складываются они так
 *  же. RelativeOffset = 1 ставит копии ВПРИТЫК: соседние копии касаются, но не
 *  наезжают, что и делает единицу осмысленным значением по умолчанию.
 *
 *  Отдельная структура, а не поля в FStateGeneratorParams, потому что тираж -
 *  не генератор: генераторы строят набор из ничего и не читают состояние сцены
 *  (см. doc-comment StateGenerators), а этот работает НАД уже существующими
 *  клетками. Общего у них только выход - массив клеток, который заливает
 *  RebuildGridFromCells(). */
USTRUCT(BlueprintType)
struct FCellArrayParams
{
	GENERATED_BODY()

	/** Сколько копий по каждой оси, СЧИТАЯ ОРИГИНАЛ (как Count в Blender):
	 *  1 - ось не размножается вовсе, 3 - оригинал плюс две копии. Итоговое
	 *  число копий - произведение трёх компонент, так что (4,4,4) это уже 64
	 *  копии исходного набора; от перегрузки защищает
	 *  AAutomataOrchestrator::MaxGeneratedCells, проверяемый ДО заливки. */
	UPROPERTY(EditAnywhere, BlueprintReadWrite, Category = "Automata|Array",
			  meta = (ClampMin = "1", UIMin = "1", UIMax = "32"))
	FIntVector Count = FIntVector(3, 1, 1);

	/** Шаг между копиями В ДОЛЯХ ГАБАРИТА исходного набора по той же оси.
	 *  1 - копии впритык (край к краю), 2 - через промежуток шириной в сам
	 *  набор, 0.5 - копии наезжают половиной.
	 *
	 *  Именно доля габарита, а не клетки: тираж почти всегда хочется "впритык"
	 *  или "через одну", а для этого нужно знать размер набора, который меняется
	 *  с каждым новым выделением. Абсолютная добавка - ConstantOffset ниже,
	 *  и они складываются. Отрицательное значение размножает в минус по оси. */
	UPROPERTY(EditAnywhere, BlueprintReadWrite, Category = "Automata|Array",
			  meta = (UIMin = "-2.0", UIMax = "4.0"))
	FVector RelativeOffset = FVector(1.0, 1.0, 1.0);

	/** Добавка к шагу В КЛЕТКАХ, поверх RelativeOffset. Ноль - копии ровно так,
	 *  как задаёт доля габарита; +1 - зазор в одну пустую клетку между копиями
	 *  (при RelativeOffset = 1), что и есть разница между "срослись в одно тело"
	 *  и "стоят рядом" для правила, считающего соседей. */
	UPROPERTY(EditAnywhere, BlueprintReadWrite, Category = "Automata|Array",
			  meta = (UIMin = "-32", UIMax = "32"))
	FIntVector ConstantOffset = FIntVector(0, 0, 0);

	/** Раскладывать копии СИММЕТРИЧНО вокруг исходного набора, а не наращивать
	 *  их в плюс от него, как делает Blender.
	 *
	 *  Разница чисто в удобстве и она заметная: камера в этот момент смотрит
	 *  ровно на исходный набор, и тираж, растущий в одну сторону, уезжает из
	 *  кадра тем дальше, чем больше Count. Со сдвигом центр тиража остаётся там
	 *  же, где был источник. При чётном Count идеального центра не существует -
	 *  сдвиг округляется вниз, то есть тираж оказывается на полшага смещён в
	 *  минус. */
	UPROPERTY(EditAnywhere, BlueprintReadWrite, Category = "Automata|Array")
	bool bCenterOnSource = true;
};
