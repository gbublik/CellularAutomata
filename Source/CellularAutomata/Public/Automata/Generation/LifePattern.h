#pragma once

#include "CoreMinimal.h"
#include "LifePattern.generated.h"

/** Классические двумерные паттерны Game of Life, которые генератор
 *  EStateGeneratorType::LifePattern выдавливает в 3D (см. его doc-comment и
 *  docs/generation.md).
 *
 *  ЗАЧЕМ ОНИ ЗДЕСЬ. Двумерная жизнь переносится в трёхмерную ТОЧНО, если
 *  выдавить паттерн ровно на два слоя и перевести правило по формуле
 *  S3D = {2s+1}, B3D = {2b} - для Конвея B3/S23 это 5,7/6 (готовый пресет в
 *  RulePresets). Считается она из простого наблюдения: в двухслойной призме
 *  клетка видит своих соседей ДВАЖДЫ (свой слой и соседний) плюс собственного
 *  двойника, живого ровно тогда, когда жива она сама. Отсюда 2N+1 у живой и
 *  2N у мёртвой.
 *
 *  Проверено на практике в этом проекте: глайдер полетел, пульсар отпульсировал
 *  и распался в те же восемь караваев, что и в 2D.
 *
 *  ГРАНИЦА У ЭТОГО ЕСТЬ, и она одна: клетка в слое НАД призмой видит только
 *  соседний слой, то есть N соседей плюс двойника. Она родится, если это даст
 *  число из B3D - для Конвея шесть. Значит паттерн остаётся плоским ровно до
 *  тех пор, пока в нём нет пустой клетки с шестью живыми соседями; проверяется
 *  это заранее гистограммой соседей (Ctrl+Y, колонка "примыкающие пустые").
 *
 *  Толщину задаёт FStateGeneratorParams::Thickness. Двойка - не значение по
 *  умолчанию из вкуса, а единственная толщина, при которой перенос точен: при
 *  одном слое соседей по Z нет вовсе, при трёх средний слой видит вдвое больше
 *  соседей, чем крайние, и расходится с ними на первом же шаге. */
UENUM(BlueprintType)
enum class ELifePattern : uint8
{
	/** Глайдер - 5 клеток, ползёт по диагонали с периодом 4. Самый маленький
	 *  транслятор и первое, на чём стоит проверять правило. */
	Glider,

	/** Лёгкий космический корабль (LWSS) - 9 клеток, идёт ПРЯМО, а не по
	 *  диагонали, с периодом 4. */
	LightweightSpaceship,

	/** Пульсар - 48 клеток, осциллятор периода 3 и самый крупный из
	 *  общеизвестных. Хорош как проверка: у него высокая симметрия, поэтому
	 *  сбой правила виден сразу, а не через сотню поколений. */
	Pulsar,

	/** Ружьё Госпера - 36 клеток, первый найденный паттерн с НЕОГРАНИЧЕННЫМ
	 *  ростом: каждые 30 поколений выпускает глайдер. В 3D это поток
	 *  двухслойных глайдеров, летящих строем.
	 *
	 *  Он же и главная проверка порога плотности: у ружья есть тесные места, и
	 *  если хоть у одной пустой клетки наберётся шесть соседей, оно начнёт
	 *  расти вверх вместо того, чтобы стрелять. */
	GosperGliderGun
};
