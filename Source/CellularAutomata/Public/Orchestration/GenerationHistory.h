#pragma once

#include "CoreMinimal.h"
#include "GenerationHistory.generated.h"

/** Одна точка графика поколений: сколько клеток было живо во всей сетке и
 *  сколько реально уехало в AddInstances() после всех фильтров (куб отсечения,
 *  фильтр по возрасту, срез).
 *
 *  Хранится ПАРОЙ с номером поколения, а не одним значением с неявным X по
 *  индексу массива, и это принципиально: один заход StepAsync() при GPU-батчинге
 *  считает сразу пачку поколений (см. AAutomataOrchestrator::
 *  LastDispatchGenerations), причём размер пачки подрезается на лету по объёму
 *  AABB. Замеры в окне стоят неравномерно, и раскладка по индексу сжимала бы и
 *  растягивала кривую при каждой смене размера пачки.
 *
 *  ВНИМАНИЕ: RenderedCount может ЗАКОННО превышать AliveCount - при правилах
 *  Generations (States > 2) угасающие клетки рисуются, но живыми не считаются
 *  (см. doc-comment FCellRenderStats). Ничто в масштабировании графика не имеет
 *  права считать одну линию заведомо выше другой. */
USTRUCT(BlueprintType)
struct FGenerationSample
{
	GENERATED_BODY()

	UPROPERTY(BlueprintReadOnly, Category = "Automata|HUD")
	int64 Generation = 0;

	UPROPERTY(BlueprintReadOnly, Category = "Automata|HUD")
	int32 AliveCount = 0;

	/** Может быть ПЕРЕНЕСЁННЫМ с предыдущего замера, а не измеренным на этом
	 *  поколении - см. Append(). */
	UPROPERTY(BlueprintReadOnly, Category = "Automata|HUD")
	int32 RenderedCount = 0;
};

/** Скользящее окно последних Capacity ЗАМЕРОВ для графика поколений на HUD -
 *  первое в проекте хранилище истории (всё остальное - FRenderTimings,
 *  FCellRenderStats, FHudStats - снимки последнего момента).
 *
 *  Свободные функции над чужим массивом, без UObject и без актора - тот же
 *  идиом, что CellAging/CellSelection/CellMeshBuilder, и по той же причине:
 *  вся неочевидная логика (перенос значения, срез по ёмкости, правка последнего
 *  замера на месте) покрывается автотестами headless, без PIE и рендера.
 *  Владеет массивом AAutomataOrchestrator (GenerationSamples), здесь только
 *  правила работы с ним.
 *
 *  Ёмкость считается в ЗАМЕРАХ, а не в поколениях: один заход может посчитать
 *  пачку, так что в поколениях окно шире ровно на её размер.
 *
 *  Потокобезопасности нет и не требуется: все записи идут с игрового потока
 *  (продолжения AsyncTask(GameThread) в ApplyStepResult()/Next() и функции
 *  рендера), отрисовка виджета - тоже. */
namespace GenerationHistory
{
	/** Замер на новое поколение.
	 *
	 *  RenderedCount ПЕРЕНОСИТСЯ с предыдущего замера (0, если истории нет):
	 *  при StepsPerRender > 1 и GPU-батчинге большинство поколений на экран не
	 *  попадают вовсе, и линия "видимо" держит последнее известное значение
	 *  ступенькой. Альтернатива - разрывы в линии - при StepsPerRender 256
	 *  оставила бы от неё редкие точки. */
	CELLULARAUTOMATA_API void Append(TArray<FGenerationSample>& History,
		int64 Generation, int32 AliveCount, int32 Capacity);

	/** Сколько инстансов реально ушло в AddInstances().
	 *
	 *  Если последний замер - про ЭТО ЖЕ поколение, правит его НА МЕСТЕ, а не
	 *  добавляет новый. Это несущая часть, а не оптимизация: рендер зовётся и
	 *  без смены поколения - RefreshRenderCullVolume() дёргается из Tick() на
	 *  каждое движение камеры при включённом срезе, DeleteSelectedCells()
	 *  правит сетку прямо на паузе. Без правки на месте одно поколение
	 *  размазалось бы на сотни точек по X от одного полёта камеры.
	 *
	 *  Если истории нет или последний замер про другое поколение - добавляет
	 *  замер. Так засевается поколение 0 сразу после ResetGenerationCounter():
	 *  все пять путей "начать прогон заново" чистят историю и тут же рисуют. */
	CELLULARAUTOMATA_API void NoteRendered(TArray<FGenerationSample>& History,
		int64 Generation, int32 AliveCount, int32 RenderedCount, int32 Capacity);

	/** Выбросить замеры о поколениях ПОЗЖЕ Generation, оставив само Generation.
	 *
	 *  Нужно ровно одному вызывающему - AAutomataOrchestrator::StepBackward()
	 *  (Ctrl+Z): шаг назад не начинает прогон заново, поэтому ResetGenerationCounter()
	 *  с его безусловным GenerationSamples.Reset() тут не годится - история до
	 *  точки отката остаётся верной и должна пережить откат, иначе график
	 *  обнулялся бы на каждое нажатие.
	 *
	 *  Не бинарный поиск, хотя массив отсортирован по Generation: обрезается
	 *  хвост окна в несколько тысяч замеров, и делает это ручное нажатие, а не
	 *  каждый кадр. Линейного прохода тут достаточно, и он не может разойтись
	 *  с порядком, если Append() когда-нибудь начнёт вставлять иначе. */
	CELLULARAUTOMATA_API void TrimAfter(TArray<FGenerationSample>& History, int64 Generation);

	/** Значение замера в том виде, в каком оно ложится на график.
	 *
	 *  Exponent <= 0 - без нормировки, вернётся сам Count. Иначе Count/n^Exponent:
	 *  голая популяция растущей фигуры идёт как объём, ~n^3, и её график - это
	 *  парабола-переросток, одинаковая у любого растущего правила. Деление
	 *  убирает главный член, и остаётся то, что он прятал: выходит ли отношение
	 *  на константу (плотная фигура), уползает ли в ноль (фрактальная губка) или
	 *  колеблется с периодом по log2(n) (точное самоподобие - та самая причина,
	 *  по которой чистая картинка у фрактальных правил выпадает на 2^k).
	 *
	 *  Показатель считается ОТ НОМЕРА ПОКОЛЕНИЯ, а не от радиуса фигуры. Пока
	 *  фронт расползается с постоянной скоростью (R ~ v*n, а быстрее светового
	 *  конуса автомат не умеет), скорость уходит в константу нормировки и
	 *  показатель равен размерности структуры. Для правил, где фронт
	 *  останавливается, это уже не так - там отношение просто падает.
	 *
	 *  Поколение 0 при включённой нормировке НЕОПРЕДЕЛЕНО (деление на ноль), и
	 *  такие замеры выбрасываются везде - и из границ, и из точек. */
	CELLULARAUTOMATA_API double NormalizedValue(int64 Generation, int32 Count, double Exponent);

	/** Показатель роста, подогнанный по окну: наклон log(AliveCount) по log(n),
	 *  обычный МНК. Это и есть измерение - число, которое имеет смысл вписать в
	 *  нормировку, чтобы кривая легла горизонтально.
	 *
	 *  Считается по ЖИВЫМ, не по видимым: "видимо" зависит от куба отсечения и
	 *  фильтров, то есть от того, куда смотрит камера, а не от правила.
	 *
	 *  0.0, если подогнать не по чему (меньше двух замеров с n > 0 и Count > 0,
	 *  либо все они на одном поколении). */
	CELLULARAUTOMATA_API double FitGrowthExponent(const TArray<FGenerationSample>& History);

	/** Границы окна для масштабирования. false, если история пуста или (при
	 *  включённой нормировке) в ней не осталось ни одного пригодного замера -
	 *  рисовать нечего. OutMinY/OutMaxY - по ОБОИМ рядам, см. FGenerationSample.
	 *
	 *  Exponent передаётся тот же, что потом уйдёт в MapToPoints(): границы
	 *  обязаны быть посчитаны по тем же значениям, которые будут нарисованы. */
	CELLULARAUTOMATA_API bool ComputeBounds(const TArray<FGenerationSample>& History, double Exponent,
		int64& OutMinGeneration, int64& OutMaxGeneration, double& OutMinY, double& OutMaxY);

	/** Отрезок оси, накрывающий [MinValue, MaxValue] с запасом и выровненный по
	 *  "красивому" шагу (1/2/5 * 10^k) - для нормированного графика, где ноль по
	 *  Y не годится в качестве дна.
	 *
	 *  Обычному графику населения дно в нуле обязательно (иначе кривая врёт о
	 *  том, во сколько раз что-то выросло), а нормированному - наоборот: всё
	 *  значение лежит в узкой полосе вроде [0.9, 1.33], и от нуля она выглядит
	 *  прямой линией. Выравнивание по шагу здесь заодно работает гистерезисом:
	 *  отрезок меняется, только когда данные переходят через границу шага, а не
	 *  на каждое дрожание. */
	CELLULARAUTOMATA_API void ComputeNiceRange(double MinValue, double MaxValue,
		double& OutMin, double& OutMax);

	/** Замеры -> точки в локальных координатах виджета (начало - левый верхний
	 *  угол, Y вниз, поэтому значение инвертируется). Чистая математика на
	 *  FVector2f, без Slate - тестируется headless вместе с остальным.
	 *
	 *  MinY/MaxY - уже сглаженный "красивый" отрезок оси, а не сырые границы из
	 *  ComputeBounds(): пересчёт сырых границ на каждом кадре заставляет всю
	 *  кривую непрерывно дышать. Значения вне отрезка подрезаются.
	 *
	 *  bLogScale и MinY несовместимы: логарифм включают ради размаха в порядки,
	 *  а поднятое дно - ради узкой полосы, и это ровно противоположные случаи.
	 *  В логарифме дно всегда ноль, MinY игнорируется. */
	CELLULARAUTOMATA_API void MapToPoints(const TArray<FGenerationSample>& History,
		const FVector2f& PlotSize, const FVector2f& PlotOrigin,
		int64 MinGeneration, int64 MaxGeneration, double MinY, double MaxY,
		bool bLogScale, double Exponent,
		TArray<FVector2f>& OutAlivePoints, TArray<FVector2f>& OutRenderedPoints);

	/** Ближайший сверху "красивый" потолок (1/2/5 * 10^k) - чтобы подпись оси
	 *  читалась и чтобы масштаб не менялся от каждой мелкой ряби. */
	CELLULARAUTOMATA_API double NiceCeiling(double Value);
}
