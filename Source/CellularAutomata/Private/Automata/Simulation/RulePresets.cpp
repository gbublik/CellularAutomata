#include "Automata/Simulation/RulePresets.h"

namespace RulePresets
{
	namespace
	{
		/** Строит пресет, пересчитывая "плотность заполнения" в число
		 *  стартовых клеток (AAutomataOrchestrator::Amount).
		 *
		 *  Источник таблицы - williamyang98/3D-Cellular-Automata,
		 *  src/app/entries/system_entries.js: там каждый пресет задан
		 *  правилом плюс "рандомайзером" вида (Density, Radius), где Density -
		 *  доля клеток шара, которую надо засеять. У нас же GenerateRandom()
		 *  принимает не долю, а абсолютное Amount, и сеет reject-sampling'ом:
		 *  Amount случайных точек в шаре, округлённых до клетки, БЕЗ проверки
		 *  на повтор - две точки часто падают в одну клетку. Поэтому Amount =
		 *  Density * Объём даёт заметно меньшую реальную заполненность, чем
		 *  задумано в источнике (особенно на больших Density).
		 *
		 *  Считаем поэтому по формуле "задачи о коллекционере": чтобы покрыть
		 *  долю p из N ячеек случайными бросками с повторами, нужно в среднем
		 *  N * ln(1/(1-p)) бросков. Density = 1.0 при этом даёт бесконечность,
		 *  поэтому p зажата сверху в 0.95 (полное заполнение шара случайными
		 *  бросками недостижимо в принципе - только перебором клеток, а это
		 *  уже другой алгоритм генерации, не тот, что в GenerateRandom()).
		 *
		 *  N берём как объём шара 4/3*pi*r^3 - для решётки это оценка числа
		 *  клеток в шаре радиуса r, точная в пределе и достаточная здесь:
		 *  Amount в любом случае лишь стартовая подсказка, дизайнер правит его
		 *  в панели. */
		FRulePreset MakePreset(const TCHAR* Name, const TCHAR* RuleString, int32 SpawnRadius, double Density)
		{
			const double CellsInSphere = 4.0 / 3.0 * PI * FMath::Pow(static_cast<double>(SpawnRadius), 3.0);
			const double CoverFraction = FMath::Min(Density, 0.95);
			const double Draws = CellsInSphere * FMath::Loge(1.0 / (1.0 - CoverFraction));

			FRulePreset Preset;
			Preset.Name = Name;
			Preset.RuleString = RuleString;
			Preset.SpawnRadius = SpawnRadius;
			Preset.Amount = FMath::Max(1, FMath::RoundToInt(Draws));
			return Preset;
		}

		/** Помечает пресет как привязанный к решётке - ApplyRulePreset() включит
		 *  её перед применением правила. Отдельная обёртка, а не два лишних
		 *  параметра у MakePreset(): привязка есть у одного пресета из
		 *  пятнадцати, и в таблице она должна бросаться в глаза, а не теряться
		 *  среди дефолтных значений в конце строки. */
		FRulePreset RequireShape(FRulePreset Preset, ECellShape Shape)
		{
			Preset.bRequiresCellShape = true;
			Preset.RequiredCellShape = Shape;
			return Preset;
		}
	}

	const TArray<FRulePreset>& GetAll()
	{
		// Порядок и имена - как в system_entries.js источника (см. выше), чтобы
		// пресет в HUD можно было один в один сверить с сайтом. "Builder 1" и
		// "Builder 2" там действительно заданы одинаковым правилом и одинаковым
		// рандомайзером - это не опечатка при переносе, дубликат есть в самом
		// источнике; оставлен как есть, чтобы нумерация пресетов совпадала.
		//
		// Радиус: у Clouds 1 и Slow Decay в источнике он задан не в клетках, а
		// долей от размера сетки (Randomiser_Radius_Relative), что у нас не с
		// чем сопоставить - GridSize в этом проекте на генерацию не влияет
		// вовсе (см. GenerateRandom(): сеет по SpawnRadius, GridSize только
		// пишется в сейв). Поэтому им проставлен конкретный радиус 30 - шар
		// примерно на 113k клеток, обычный для этого проекта масштаб старта.
		static const TArray<FRulePreset> Presets = {
			MakePreset(TEXT("Amoeba-1"),         TEXT("9-26/5-7,12-13,15/16/M"),   8,  0.3),
			MakePreset(TEXT("445"),              TEXT("4/4/5/M"),                  15, 0.1),
			MakePreset(TEXT("Builder 2"),        TEXT("6,9/4,6,8-9/10/M"),         7,  0.35),
			MakePreset(TEXT("Crystal Growth 1"), TEXT("0-6/1,3/2/VN"),             1,  1.0),
			MakePreset(TEXT("Crystal Growth 2"), TEXT("1-3/1-3/5/VN"),             1,  1.0),
			MakePreset(TEXT("Clouds 1"),         TEXT("13-26/13-14,17-19/2/M"),    30, 0.5),
			MakePreset(TEXT("Slow Decay"),       TEXT("8,11,13-26/13-26/5/M"),     30, 0.45),
			MakePreset(TEXT("Spiky Growth 1"),   TEXT("7-26/4,12-13,15/10/M"),     7,  0.32),
			MakePreset(TEXT("Ripple Cube"),      TEXT("8-26/4,12-13,15/10/M"),     10, 0.35),
			MakePreset(TEXT("Amoeba-2"),         TEXT("9-26/5-7,12-13,15/5/M"),    5,  0.3),
			MakePreset(TEXT("Builder 1"),        TEXT("6,9/4,6,8-9/10/M"),         7,  0.35),
			MakePreset(TEXT("Pyroclastic"),      TEXT("4-7/6-8/10/M"),             5,  0.2),
			MakePreset(TEXT("678 678"),          TEXT("6-8/6-8/3/M"),              5,  0.3),

			// --- Дальше идёт НЕ источник: находки этого проекта. ---
			//
			// Держим их строго после всего блока выше, чтобы нумерация первых
			// тринадцати продолжала совпадать с system_entries.js один в один -
			// вставка своего пресета в середину сломала бы ровно ту сверку с
			// сайтом, ради которой сохранён порядок источника.
			//
			// Crystal Growth 1 (Moore): от каталогизированного Crystal Growth 1
			// отличается ОДНОЙ буквой - окрестностью. Рождения и выживания те же,
			// но Moore вместо von Neumann, и этого достаточно, чтобы сменилась
			// геометрия роста: под Moore диагонали идут с той же скоростью, что
			// и оси (метрика Чебышёва), фронт растёт кубически и даёт прямые
			// лучи с диагональными ответвлениями; под von Neumann метрика
			// манхэттенская, фронт октаэдрический, и получаются грани, а не
			// лучи. Ядро этого автомата читается как симметричный крест - на
			// нём и держится подборка кандидатов в логотип проекта.
			//
			// Параметры засева взяты от VN-версии намеренно: правила роста
			// кристалла стартуют с крошечного зерна, а вся интересная геометрия
			// - свойство самого правила, а не начального пятна.
			MakePreset(TEXT("Crystal Growth 1 (Moore)"), TEXT("0-6/1,3/2/M"),      1,  1.0),

			// Плоская жизнь: ГЕРМЕТИЧНЫЙ перенос двумерного Конвея (B3/S23) на
			// двухслойные фигуры. Числа выведены, а не подобраны: под
			// окрестностью PlanarMoore (плоскость XY плюс ось Z, без вертикальных
			// диагоналей) клетка двухслойной призмы видит N соседей своего слоя
			// плюс собственного двойника, то есть N+1 у живой и N у мёртвой -
			// отсюда S = {2+1, 3+1} = {3,4} и B = {3}.
			//
			// Герметичность в том, что клетка НАД призмой видит из неё ровно
			// одного соседа - двойника под собой - и при B={3} не родится
			// никогда. Два слоя не могут стать тремя, поэтому переносится любой
			// 2D-паттерн.
			//
			// Первая версия этого пресета была "5,7/6/2/M" - тот же вывод, но
			// под Moore-26, где клетка над призмой видит девять клеток
			// соседнего слоя. Она работала только на разреженных паттернах:
			// глайдер летел (максимум 5 при пороге 6), а пульсар и ружьё
			// Госпера, у которых ровно 6, обрастали вверх на первом же шаге.
			// Замерено, а не предположено - см. docs/generation.md.
			//
			// Строит фигуры генератор EStateGeneratorType::LifePattern. Засев
			// случайным шаром смысла почти не имеет - правило живёт на плоских
			// паттернах, - но пресет обязан задать ВСЕ свои поля (конвенция
			// таблиц пресетов), поэтому радиус маленький.
			//
			// ЕДИНСТВЕННЫЙ пресет каталога, привязанный к решётке, и привязан он
			// не из вкуса. Соседство PlanarMoore существует только как оболочка,
			// а BuildRule() отдаёт приоритет списку смещений формы, когда та его
			// задаёт (NeighborhoodShape != Shells): на удлинённом додекаэдре
			// токен /PM был бы разобран, записан в поле и молча не применён -
			// осталось бы только B/S, то есть совсем другое правило под тем же
			// именем. Плюс сама герметичность переноса 2D-паттернов выведена для
			// клетки-куба и на подрешётке не держится.
			RequireShape(MakePreset(TEXT("Плоская жизнь (Conway 2D)"), TEXT("3,4/3/2/PM"), 4, 0.3),
				ECellShape::Cube),
		};

		return Presets;
	}
}